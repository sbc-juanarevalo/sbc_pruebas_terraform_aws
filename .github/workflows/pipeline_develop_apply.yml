name: Develop Apply
on:
  pull_request:
    branches: [develop]
    types: [closed]

jobs:
  Apply:
    name: "Apply"
    if: ${{ github.event.pull_request.merged }}
    runs-on: ubuntu-20.04
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID_DEVELOP_ORBIKA }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY_DEVELOP_ORBIKA }}
    defaults:
      run:        
        #working-directory: src
        working-directory: config   
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      #- name: Setup Terraform
      #  uses: hashicorp/setup-terraform@v1
      #  with:
      #    terraform_version: 1.0.1
      #    terraform_wrapper: false

      #- name: Terraform Format
      #  id: fmt
      #  run: terraform fmt      

      #- name: Terraform Format check
      #  id: check
      #  run: terraform fmt -check

      #- name: Terraform Init
      #  id: init
      #  run: terraform init '-var=branch_name=dev' #'-backend-config=bucket=sbc-pruebas-terraform-aws'

      #- name: Terraform plan
      #  id: plan
      #  run: terraform plan -var="branch_name=dev"

      #- name: Terraform apply
      #  run: terraform apply -var="branch_name=dev" -auto-approve

      #- name: uploadconfig
      #  run: TEST_VAR='Hello World!
      
      - name: "Say Hello Mona it's Monday"
        run: echo "$Greeting $First_Name. Today is $DAY_OF_WEEK!"
        env:
          First_Name: Mona
          Greeting: Hola
          DAY_OF_WEEK: Monday
        
      
      - name: verRuta
        run: ls

      - name: verArchivo
        run: cat config.yaml

      - name: loadconfig
        run: clave2="algunaclave"
      
     

      - name: VerVariable
        run: echo "$Greeting $First_Name. Today is $DAY_OF_WEEK!"

   

      - name: Set variables
        run: |
          env VAR=$(cat config.yaml)
          echo "$VAR"
          env
          

      - name: VerVariable        
        run: echo "$AWS_ACCESS_KEY_ID, $AWS_DEFAULT_REGION, $DistributionID, ${{ env.DistributionID }}, $VER"
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID_DEVELOP_ORBIKA }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY_DEVELOP_ORBIKA }}
          AWS_DEFAULT_REGION: "Global"
          DistributionID: '"E1DNJ6PRKN7113"'
      

      #- name: AWS CLI v2              
      #  uses: imehedi/actions-awscli-v2@latest
      #  with:          
      #    args: cloudfront create-invalidation --distribution-id '"${{ env.DistributionID }}"' --paths "/index.html"
      #  env:
      #    AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID_DEVELOP_ORBIKA }}
      #    AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY_DEVELOP_ORBIKA }}
      #    AWS_DEFAULT_REGION: "Global"
          #DistributionID: '"E1DNJ6PRKN7113"'
          #DistributionID: "E1DNJ6PRKN7113"
          #DistributionID: 'E1DNJ6PRKN7113'
          #DistributionID: E1DNJ6PRKN7113
          #DistributionID: "'E1DNJ6PRKN7113'"

      
          

      